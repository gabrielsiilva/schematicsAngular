import { HoraDataHoraPipe } from './hora-data-hora.pipe';

describe('HoraDataHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new HoraDataHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
